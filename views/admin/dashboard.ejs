<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <link rel="apple-touch-icon" sizes="76x76" href="../assets/img/apple-icon.png">
  <link rel="icon" type="image/png" href="../assets/img/favicon.png">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <title>
    Admin Dashboard
  </title>
  <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, shrink-to-fit=no'
    name='viewport' />
  <!--     Fonts and icons     -->
  <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,200" rel="stylesheet" />
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css"
    integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
  <!-- CSS Files -->
  <link href="/admin/assets/css/bootstrap.min.css" rel="stylesheet" />
  <link href="/admin/assets/css/admin.css" rel="stylesheet" />

  <link href="/admin/assets/demo/demo.css" rel="stylesheet" />
</head>

<body class="">
  <div class="wrapper ">
    <!---- Sidebar start -->
    <%- include('../partials/sidebar') %>
      <!---- Sidebar end -->
      <div class="main-panel" id="main-panel">
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg navbar-transparent  bg-primary  navbar-absolute">
          <div class="container-fluid">
            <div class="navbar-wrapper">
              <div class="navbar-toggle">
                <button type="button" class="navbar-toggler">
                  <span class="navbar-toggler-bar bar1"></span>
                  <span class="navbar-toggler-bar bar2"></span>
                  <span class="navbar-toggler-bar bar3"></span>
                </button>
              </div>
              <a class="navbar-brand" href="#pablo">Dashboard</a>
            </div>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation"
              aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-bar navbar-kebab"></span>
              <span class="navbar-toggler-bar navbar-kebab"></span>
              <span class="navbar-toggler-bar navbar-kebab"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navigation">
              <ul class="navbar-nav">
                <li class="nav-item dropdown">
                  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="#">Reports</a>
                    <a class="dropdown-item" href="#">Settings</a>
                    <a class="dropdown-item" href="#">Logout</a>
                  </div>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="#pablo">
                    <i class="now-ui-icons users_single-02"></i>
                    <p>
                      <span class="d-lg-none d-md-block">Account</span>
                    </p>
                  </a>
                </li>
              </ul>
            </div>
          </div>
        </nav>
        <!-- End Navbar -->
        <div class="panel-header panel-header-lg">
          <canvas id="bigDashboardChart"></canvas>
        </div>

        <!-- Analytics card -->
        <div class="content">
          <div class="row">
            <div class="col-lg-4 col-md-6">
              <div class="card card-chart">
                <div class="card-header">
                  <h5 class="card-category">Monthly Chart</h5>
                  <h4 class="card-title">Visitor Log</h4>
                  <div class="dropdown">
                    <button type="button"
                      class="btn btn-round btn-outline-default dropdown-toggle btn-simple btn-icon no-caret"
                      data-toggle="dropdown">
                      <i class="now-ui-icons loader_gear"></i>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right">
                      <a class="dropdown-item" href="#">Analytics</a>
                      <a class="dropdown-item" href="#">Analytics Report 1</a>
                      <a class="dropdown-item" href="#">Analytics Report 2</a>
                      <a class="dropdown-item text-danger" href="#">Remove Data</a>
                    </div>
                  </div>
                </div>
                <div class="card-body">
                  <div class="chart-area">
                    <canvas id="lineChartExample"></canvas>
                  </div>
                </div>
                <div class="card-footer">
                  <div class="stats">
                    <i class="now-ui-icons arrows-1_refresh-69"></i> Just Updated
                  </div>
                </div>
              </div>
            </div>

            <div class="col-lg-4 col-md-6">
              <div class="card card-chart">
                <div class="card-header">
                  <h5 class="card-category">Daily Analysis</h5>
                  <h4 class="card-title">Visitor/Staff Inflow</h4>
                </div>
                <div class="card-body">
                  <div class="chart-area">
                    <canvas id="barChartSimpleGradientsNumbers"></canvas>
                  </div>
                </div>
                <div class="card-footer">
                  <div class="stats">
                    <i class="now-ui-icons ui-2_time-alarm"></i> Last 7 days
                  </div>
                </div>
              </div>
            </div>

            <div class="col-lg-4 col-md-6">
              <div class="card metric-card">
                <div class="card-header">
                  <h5 class="card-category">Staff Attendance</h5>
                </div>
                <div class="card-body">
                  <div class="card my-card has-background-gradient-teal">
                    <div class="my-auto mx-auto">
                      <span class="fa fa-users my-icon py-3 px-4"></span>
                    </div>
                    <div class="my-auto px-4">
                      <p class="mb-1">Total Staff no.</p>
                      <h2>
                        <% if (typeof totalStaffCount !=='undefined' ) { %>
                          <%= totalStaffCount %>
                            <% } %>
                      </h2>
                    </div>

                  </div>

                  <div class="card my-card has-background-gradient-blue">
                    <div class="my-auto mx-auto">
                      <span class="fa fa-users my-icon py-3 px-4"></span>
                    </div>
                    <div class="my-auto px-4">
                      <p class="mb-1">Clock-in Today</p>
                      <h2>2435</h2>
                    </div>
                  </div>

                  <div class="card my-card has-background-gradient-orange">
                    <div class="my-auto mx-auto">
                      <span class="fa fa-users my-icon py-3 px-4"></span>
                    </div>
                    <div class="my-auto px-4">
                      <p class="mb-1">Absent Today</p>
                      <h2>2435</h2>
                    </div>

                  </div>
                </div>

                <div class="card-footer">
                  <div class="stats">
                    <i class="now-ui-icons arrows-1_refresh-69"></i> Just Updated
                  </div>
                </div>
              </div>

            </div>
          </div>
        </div>

        <!-- Log table -->
        <div class="content">
          <div class="row">


            <div class="col-lg-12 col-md-12">
              <div class="card card-chart">
                <div class="card-header">
                  <h5 class="card-category">Staff Attendance</h5>
                  <h4 class="card-title">Attendance Metrics</h4>
                  <% if (!attendanceData) { %>
                    <div class="alert alert-danger">Attendance data is missing!</div>
                    <% } else { %>
                      <div class="alert alert-success">Attendance data loaded successfully.</div>
                      <% } %>
                </div>
                <div class="card-body" id="attendance-table">
                  <div class="table-responsive-md">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                      <!-- Week Navigation Buttons -->

                      <!-- Previous Weeks Buttons -->
                      <a href="?offset=<%= week.offset + 1 %>&page=<%= pagination.current %>#attendance-table"
                        class="btn btn-outline-primary">
                        ← Previous Week
                      </a>

                      <!----Skip to current week -->
                      <% if (week.offset> 0) { %>
                        <a href="?offset=0&page=<%= pagination.current %>#attendance-table" class="btn btn-info">
                          📅 Current Week
                        </a>
                        <% } %>

                          <!-- Next Week Button, will only appear when admin clicks previous week button -->
                          <% if (week.offset> 0) { %>
                            <a href="?offset=<%= week.offset - 1 %>&page=<%= pagination.current %>#attendance-table"
                              class="btn btn-outline-success">
                              Next Week →
                            </a>
                            <% } %>
                    </div>

                    <div class="row justify-content-between align-items-end mb-3 log-filter">
                      <!-- Left Column: Search + Clear -->
                      <div class="col-md-7 col-sm-6">
                        <form method="GET" class="d-flex gap-2 align-items-center">
                          <input type="hidden" name="offset" value="<%= week.offset %>">
                          <input type="hidden" name="page" value="1">
                          <input type="text" name="search" class="form-control-sm flex-grow-1"
                            placeholder="Search staff name..." value="<%= search || '' %>">
                          <button type="submit" class="btn btn-primary">Search</button>
                          <!-- <a href="" class="btn btn-secondary">Clear Filters</a> -->
                        </form>
                      </div>

                      <!-- Right Column: Date Picker + Go Button -->
                      <div class="col-md-5 col-sm-6">
                        <div class="d-flex gap-2 justify-content-end align-items-center">
                          <input type="date" id="datePicker" class="form-control-sm w-auto flex-grow-1">
                          <button id="jumpToDateBtn" class="btn btn-secondary"><i class="now-ui-icons ui-1_calendar-60"></i></button>
                           <a href="/admin/dashboard" class="btn btn-secondary">Clear Filters</a>
                        </div>
                      </div>
                    </div>

                    <table class="table table-bordered text-center">
                      <thead class="thead-dark">
                        <tr>
                          <th>Name</th>
                          <% weekDays.forEach(day=> { %>
                            <th>
                              <%= day.name %><br>
                                <small>
                                  <%= day.date %>
                                </small>
                            </th>
                            <% }) %>
                              <th>Total</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% attendanceData.forEach(staff=> { %>
                          <tr>
                            <td>
                              <%= staff.name %>
                            </td>
                            <% ['Monday','Tuesday','Wednesday','Thursday','Friday'].forEach(day=> { %>
                              <td>
                                <% if (staff.attendance[day]) { %>
                                  ✅
                                  <% } else { %>
                                    ❌
                                    <% } %>
                              </td>
                              <% }) %>
                                <td>
                                  <%= staff.total %>
                                </td>
                          </tr>
                          <% }) %>
                      </tbody>
                    </table>
                    <!-- Pagination Controls -->
                    <div class="pagination-container">
                      <ul class="pagination">
                        <% if (pagination.current> 1) { %>
                          <li><a
                              href="?page=<%= pagination.current - 1 %>&offset=<%= week.offset %><%= search ? `&search=${encodeURIComponent(search)}` : '' %>#attendance-table">Prev</a>
                          </li>
                          <% } else { %>
                            <li class="disabled">Prev</li>
                            <% } %>

                              <% for (let i=1; i <=pagination.total; i++) { %>
                                <% if ( i===1 || i===pagination.total || Math.abs(i - pagination.current) <=1 ) { %>
                                  <li class="<%= i === pagination.current ? 'active' : '' %>">
                                    <a
                                      href="?page=<%= i %>&offset=<%= week.offset %><%= search ? `&search=${encodeURIComponent(search)}` : '' %>#attendance-table">
                                      <%= i %>
                                    </a>
                                  </li>
                                  <% } else if ( i===2 && pagination.current> 3 ||
                                    i === pagination.total - 1 && pagination.current < pagination.total - 2 ) { %>
                                      <li>...</li>
                                      <% } %>
                                        <% } %>

                                          <% if (pagination.current < pagination.total) { %>
                                            <li><a
                                                href="?page=<%= pagination.current + 1 %>&offset=<%= week.offset %><%= search ? `&search=${encodeURIComponent(search)}` : '' %>#attendance-table">Next</a>
                                            </li>
                                            <% } else { %>
                                              <li class="disabled">Next</li>
                                              <% } %>
                      </ul>
                    </div>
                  </div>

                </div>
                <div class="card-footer">
                  <div class="stats">
                    <i class="now-ui-icons arrows-1_refresh-69"></i> Just Updated
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>


        <% if (typeof error !=='undefined' && error ) { %>
          <div class="alert alert-danger">
            <%= error %>
          </div>
          <% } %>
            <% if (typeof success !=='undefined' && success) { %>
              <div class="alert alert-success">
                <%= success %>
              </div>
              <% } %>
                <!-- Footer section -->
                <footer class="footer">
                  <div class=" container-fluid ">
                    <nav>
                      <ul>
                        <li>
                          <a href="https://www.opexconsult.com">
                            Opex consulting
                          </a>
                        </li>
                        <li>
                          <a href="http://opexconsult.com">
                            About Us
                          </a>
                        </li>
                        <li>
                          <a href="http://blog.opexconsult.com">
                            Blog
                          </a>
                        </li>
                      </ul>
                    </nav>
                    <div class="copyright" id="copyright">
                      &copy;
                      <script>
                        document.getElementById('copyright').appendChild(document.createTextNode(new Date().getFullYear()))
                      </script>, Designed by <a href="https://www.invisionapp.com" target="_blank">Opex-Team-Engrs</a>.
                      Coded
                      by
                      <a href="https://www.opexconsult.com" target="_blank">Opex Consulting</a>.
                    </div>
                  </div>
                </footer>
      </div>

  </div>
  <!--   Core JS Files   -->
  <script src="/admin/assets/js/core/jquery.min.js"></script>
  <script src="/admin/assets/js/core/popper.min.js"></script>
  <script src="/admin/assets/js/core/bootstrap.min.js"></script>
  <script src="/admin/assets/js/plugins/perfect-scrollbar.jquery.min.js"></script>
  <!--  Google Maps Plugin    -->
  <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_KEY_HERE"></script>
  <!-- Chart JS -->
  <script src="/admin/assets/js/plugins/chartjs.min.js"></script>
  <!--  Notifications Plugin    -->
  <script src="/admin/assets/js/plugins/bootstrap-notify.js"></script>
  <!-- Control Center for Now Ui Dashboard: parallax effects, scripts for the example pages etc -->
  <script src="/admin/assets/js/admin.js" type="text/javascript"></script>
  <!-- Now Ui Dashboard DEMO methods, don't include it in your project! -->
  <script src="/admin/assets/demo/demo.js" type="text/javascript"></script>
  <script>
    $(document).ready(function () {
      // Javascript method's body can be found in assets/js/demos.js
      demo.initDashboardPageCharts();
    });

    // make alert disappear after set time
    window.setTimeout(function () {
      $(".alert").fadeTo(500, 0).slideUp(500, function () {
        $(this).remove();
      });
    }, 4000);

    //date filter
    document.getElementById('jumpToDateBtn').addEventListener('click', function () {
      const selectedDate = document.getElementById('datePicker').value;
      if (!selectedDate) return;

      const inputDate = new Date(selectedDate);
      const now = new Date();

      // Start of current ISO week
      const startOfThisWeek = new Date(now.setDate(now.getDate() - (now.getDay() === 0 ? 6 : now.getDay() - 1)));
      const startOfInputWeek = new Date(inputDate.setDate(inputDate.getDate() - (inputDate.getDay() === 0 ? 6 : inputDate.getDay() - 1)));

      // Calculate offset in weeks
      const diffInTime = startOfInputWeek.getTime() - startOfThisWeek.getTime();
      const offset = Math.round(diffInTime / (7 * 24 * 60 * 60 * 1000));

      // Redirect with new offset and anchor
      const currentPage = <%= pagination.current %>;
      window.location.href = `?offset=${offset}&page=${currentPage}#attendance-table`;
    });
  </script>
</body>

</html>